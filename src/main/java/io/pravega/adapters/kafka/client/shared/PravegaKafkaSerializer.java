package io.pravega.adapters.kafka.client.shared;


/*import org.apache.kafka.common.serialization.Deserializer;
import org.apache.kafka.common.serialization.Serializer;

import java.nio.ByteBuffer;*/

public class PravegaKafkaSerializer<T> {

    /*implements io.pravega.client.stream.Serializer<T> {

    private final Serializer<T> kSerializer;

    private final Deserializer<T> kDeserializer;

    private

    public PravegaKafkaSerializer(Serializer<T> kafkaSerializer, Deserializer<T> kafkaDeserializer) {
        this.kSerializer = kafkaSerializer;
        this.kDeserializer = kafkaDeserializer;
    }

    @Override
    public ByteBuffer serialize(T value) {
        kSerializer.serialize()
    }

    @Override
    public T deserialize(ByteBuffer serializedValue) {
        return null;
    }*/
}
