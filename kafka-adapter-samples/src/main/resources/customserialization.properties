# Kafka Servers
bootstrap.servers=localhost:32768
# Pravega servers
#bootstrap.servers=tcp://localhost:9090

# Serde
key.serializer=org.example.kafkaclient.sampleapps.serialization.PersonSerializer
value.serializer=org.example.kafkaclient.sampleapps.serialization.PersonDeserializer

# Consumer-specific properties
group.id=Consumer_Group_Id_7
client.id=org.example.client.23
auto.offset.reset=earliest

# App-specific config
topic.name=test-topic